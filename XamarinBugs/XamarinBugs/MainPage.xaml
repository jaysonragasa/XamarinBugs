<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="XamarinBugs.MainPage"
             
             x:Name="this"
             BindingContext="{Binding Source={StaticResource Locator}}"
             >

    <StackLayout>
        <Picker ItemsSource="{Binding MainPage.PickerSource}" 
                ItemDisplayBinding="{Binding DisplayName}"
                SelectedItem="{Binding BindingContext.MainPage.SelectedItem, Source={x:Reference this}}"
                >
            
        </Picker>
    </StackLayout>

</ContentPage>
